<h1>Add New User</h1>
<%= form_for :user, url: users_path do |f| %>
  <p>
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </p>

  <p>
    <%= f.label :role %><br>
    <%= f.select :role, options_for_select(['paid', 'admin']) %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
